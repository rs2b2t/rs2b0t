# dependencies
node_modules/
bun.lockb

# build output
out/
dist/
*.tsbuildinfo

# nav artifacts (baked collision packs are large; regenerated by tools/nav)
*.lcnav.gz
tools/nav/script-routes.generated.json
tools/nav/script-routes.hardest.json

# secrets & local env — never commit (keys live in ~/code/claude-workspace/.env)
.env
.env.*
*.pem

# editor / OS
.DS_Store
*.log
.idea/
.vscode/

# brainstorm visual-companion mockups
.superpowers/

# local Claude tooling scratch (agents, worktrees, session state)
.claude/

# design specs and implementation plans — working notes, never committed
docs/superpowers/

# recovered farm logins — plaintext, never commit
multibox-accounts.recovered.json

# bot wall lockfiles — local state, never commit
.b0t-launch.lock/
# live-harness proof shots — attach to PRs, do not commit multi-MB PNGs
screenshots/

# operator-only notes (not for token-hungry upstream clones)
docs/local/
